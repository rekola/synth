Use Eigen for everything

add ranges:
    auto rng = song.range(row0, col0, row1, col1);
    auto rng = song.range(SongCellReference(row0, col0), XLCellReference(row1, col1));

    // An XLCellRange object provides begin and end iterators, and can be used in
    // range-based for-loops. The next line iterates through all the cells in the
    // range and assigns a random number to each cell:
    for (auto& cell : rng) cell.value() = distr(generator);

    // Saving a large spreadsheet can take a while...
    cout << "Saving spreadsheet ..." << endl;
    doc.save();
    doc.close();

    // Reopen the spreadsheet..
    cout << "Re-opening spreadsheet ..." << endl;
    doc.open("./Demo05.xlsx");
    wks = doc.workbook().worksheet("Sheet1");

    // Create the range, count the cells in the range, and sum the numbers assigned
    // to the cells in the range.
    cout << "Reading data from spreadsheet ..." << endl;
    rng = wks.range(XLCellReference("A1"), XLCellReference(OpenXLSX::MAX_ROWS, 8));
    cout << "Cell count: " << std::distance(rng.begin(), rng.end()) << endl;
    cout << "Sum of cell values: "
         << accumulate(rng.begin(), rng.end(), 0, [](uint64_t a, XLCell& b) { return a + b.value().get<int64_t>(); });

    doc.close();

--
use templates: A1.get<double>()
How to play white color on launchpad if it only outputs single frequencies

 
- If note has been set, do not off it

- If not playing, do lowpass filtering for the aftertouch so that the selected level doesn't get cleared when user releases

Add catmull-rom interpolation for aftertouch

Add light colors to launchpad when lighting track is active

Add LightingInstrument class
 - add AUDIO_SOURCE and LIGHTING_SOURCE types

Visualisation:
 - lyrics
 - images
 - can Filter lowpass images?
 
Add search to HierachyView

Add lighting control
 - https://www.thomann.de/fi/enttec_dmx_usb_pro_interface_bundle.htm
 - https://www.thomann.de/fi/enttec_open_dmx_usb_interface.htm
 - Add lighting control to note
   + what fields?
 - Color has frequency so we can use note as the color
   + Add tuning for color
 - Tuning is pattern specific
 - Convert midi frequency to light frequency
 
We can output DMX at the same frequency as audio, and resample, but video is different matter

rename Song and SongState
 - Program?
 - Project?
 
ChannelConfiguration
 - or BusConfiguration

Keyboard input:
 - root note
 - fifth or fourth
 - chord
 - song has the key and quality
   - C major
   - natural_minor
   - harmonic_minor
   - quartal_major
   - quartal_minor

SampleData:
 - Add DMX data
 - Add MIDI data
 - Add Visual data
 - add separate AudioChannel class returned by channel(x), or audioChannel(x)
 - numberOfChannels(), numberOfAudioChannels()
 - rename to BusData, AudioBus
 - Do not interleave left right samples to improve caching for filters
    - oscilators get worse caching
    - For dynamic channel placement, interleaving is probably impossible
 - Add SampleDataRange class

Add scenes:
 - Add scene header
 - multiple patterns per scene
 - patterns can span multiple scenes
   + for example, same drum pattern can play troughout the program
 - mode: once, repeat
 
remove track states from SongState:
 - just render song state, and it renders it's InstrumentTrackState children

Test:
 - play tonic note in pattern
 - bass drones

Copy effects from Zynaddsubfx
 - https://zynaddsubfx.sourceforge.io/
 - http://yoshimi.sourceforge.net/

Add equalizer for piano => too much frequencies

add start phase to soundfont and test multiply again

midi recording leaves invisible notes

add default aftertouch to SoundFont
 - volume?
 - vibrato?
 - brightness?

add aftertouch filters
 - pitch
 - vibrato

FileInstrument
 - add modulation
 - add azimuth
 - add vocoder
 - copy linear interpolation from SoundFont
 - add recording
   - could be done from menu
 - add aftertouch to test scrubbing


- When recording, add aftertouch signals using mousewheel

Add time spacing to Multiply
Try adding switch to create_signal to see if it is optimized out

the current random phase for 44100 is a second long => too much

samplePosition should be long long and we could increment it by one all the time

Add SoundPosition class for azimuth, elevation and distance

lookup midi event handling from TinySoundFont
 - volume
 - pan
 - bank selection
 - rpn?
 - all sound off, all notes off, all ctrl off
 - data: pitch range, tuning
  
Add random phase to InstrumentTrack to play notes

Add better unit to MultiplyNote detune (similar to other synths)

Oscilator doesn't pass velocity or azimuth to children

Add speaker setup ??
 STEREO
 5.1

Add VoicingMode: MONO / POLY, LEGATO
NoteMultiplier phases should not be random

Why does LowpassFilter use doubles?
Combine Filter and LowpassFilter (as a biquad mode)

Where to handle velocity? Before distortion or after distortion?
 - For soundfont, the matter is obvious 

Combine TrackInfo and VisibleTrackInfo

InstrumentVoice::playnote might be useful when gliding notes

Get basic drum patterns from the Yamaha keyboard

add to docs:
 // modulation:	Strength of the frequency modulation
 // harmonic:	Harmonic of the modulator
 // subharmonic:	Subharmonic of the modulator
 // transpose:	Common frequency scale for carrier and modulator

Bass:
 - saw single voice
  - Add filter envelope to first play the high frequencies
 - saw five voices less filter less volume
 - soft clipping (with tan)
 
Encapsulate XML serialization and deserialization

Add Vibrato effect + envelope

Make song:
 C Db D
 G A A#
 Db D D#
  
Keep track of instrument ids for voices. If same instrument is playing same note in the same column, kill it instantly

Song should not be Track, but a NamedObject (or XMLElement or Element or Object or SongObject), since it doesn't override any Track methods and it has two sets of children and patterns

if non existing instrument is used in a track => crash

#if 0
  auto epiano = make_unique<SubtractiveInstrument>(WaveformType::SAW);
  epiano->setName("Electric Piano");
  epiano->setAmpEnvelope(Envelope(0.0f, 10 * 20 / 255.0f, 0.0f, 0.0));
  epiano->addEffect(make_unique<Filter>(63 / 255.0f, 128 / 63.0f, false));
  song->addInstrument(move(epiano));

  auto oboe = make_unique<FMInstrument>(0.7, 1, 3, 4);
  oboe->setName("oboe");
  oboe->setAmpEnvelope(Envelope(5 * 2 / 255.0f, 10 * 15 / 255.0f, 0.5f, 5 * 10 / 255.0f));
  song->addInstrument(move(oboe));  
#endif

Add predefined drum patterns => how to play them?

Combine event types: PlaybackControlEvent and TrackEvent
combine State::getRelativePosition and song.normalizePosition()
When stopping playback, notes are never offed

IF XML is malformed, the loader claims it doesn't exist
Add nonstandard soundfont instruments by name
Add soundfont browser
Look for atmospheric music sound effects
Add instrument lookup choices "Yamaha Grand Piano, piano"

Create double octave key
 - We can start with Dm, but in the second octave use D to let G keep it's D

Composition
 - harmony notes
 - passing note
 - auxiliary note
 - anticipatory note

Dynamic tuning
 - disable dynamic tuning for percussion
   - Don't use notes for percussion
     - not possible because we can switch instruments between percussion and non percussion     

Increasing sampling rate slows the song

If same note is played multiple times, the notes should not have individual decays

Add status line with dynamic tuner changes: D+1c Eb+42c

C	1:1		C major		C-E-G
C	1:1		C minor		C-Eb-G
C	1:1		C q. major	C-D#-F
D𝄫
C#
D♭
D	9:8		D minor		D-F-A
E𝄫	8:7		?		?
D#	7:6		D q. dim	D#-F-A#
Eb	6:5		Eb major	Eb-G-Bb
E	5:4		E minor		E-G-B
F	4:3		F major		F-A-C
G	3:2 = 1+1:2	G minor		G-Bb-D
G	3:2 = 1+1:2	G major		G-B-D
G	3:2 = 1+1:2	G q. major	G-A#-C
Ab ???			Ab major	Ab-C-Eb
A	5:3 = 1+2:3	A minor		A-C-E
A#	7:4 = 1+3:4	A# q. minor	A#-C-D#
Bb	9:5 = 1+4:5	Bb major	Bb-D-F
Bb	9:5 = 1+4:5	Bb q. minor	Bb-D𝄫-Eb
A𝄪4     11:6 = 1+5:6
	13:7 = 1+6:7
B	15:8 = 1+7:8	B dim		B-D-F		
B	15:8 = 1+7:8	B q. minor	B-D♭-E		
	17:9 = 1+8:9

Propagate isSolo with SampleData
  => it discards all others

Where do track and instrument names belong?
 - the InstrumentTrack or the root filter?
 -
 
should samples be outside pattern?
 - If we record on track, the recording doesn't really honor the pattern boundaries
 - If the pattern is shortened, the samples would have to be moved to another track
 - Or if the pattern runs out, where do we put the samples?
 
SoundFont:
  Implement chorus and reverb in SoundFont
    - the parameter means how much of the signal is sent to the effect
  Reverb and Chorus should be always present, but how do we differentiate between controllable reverb / chorus and always active ones
    - should they be instrument filters?
  Add modulators
    - check the original lib, or maybe fluidsynth
  What kind of lazy mixing could we do to keep the data from different regions with different settings separate
    - none, since they are instrument filters

Consider keeping effect parameters in separate structure that can be passed to states

Copy Compressor from Audacity

Add sample blocks into sampletrack when recording
Implement sample playback

How to remove Soundfont violin default vibrato?
  - it's not cool to remove it, but there might not be any choice
  - we would get double vibrato if we try to control it
  - since we only have soundfonts, we could not have vibrato at all (What?)
  - irrelevant
  
id system is not very nice
  - it can set duplicate ids
  - maybe song should have next_insert_id variable

XML saving doesn't work, since it saves only children and doesn't use ids correctly

Is there a generic way to handle grooves?
  - some separate repeating pattern with global delays
  - multiple sequences?
  - nevertheless, we would like to be able to have just a single drum pattern, that also creates the groove

What kind of algebra could we do with tracks?
  - Similar to layers in graphics
  
How to do note effects correctly?
  - They are completely dissimilar to effects
  - hard to read
  - If we have a track for lowpass filter, it will need a column, and it could have a proper effect control
    + use aftertouch for filter control

Hierarchy:
  - add song as parameter to Track::render() or InstrumentCollection
  - When scrolling down, hide extra headers
  - Add track colors to XML
    - it makes drawing harder and we cannot have black divider
  - what if we slow the time of master track or apply separate patterns
  
Can we do interpolation for events?

Try to maintain column
  - If we are in an effect column, but scroll to a pattern with more note columns, the column changes

Add backspace for effect commands.. what does it do?

Song::render crashes with an new empty song when the state was previously used

Add blitting
  - Add blit plane, the add scopes and stuff

Add effect control command

UTF8 fixes
  - Add UTF8 string
  - Add ellipsis to track name and instrument name
  - Use utf8 erase when limiting track name and instrument name
  
is loopStart or loopEnd unnecessary?

Add time signatures

Display show black initially
 - there shouldn't be anything that needs initialization.
 - Except SoundFont loading
   - add debug info
   - could it be loaded lazily?
 - genericInstrument can be loaded lazily too, but instrumentProvider must be passed to thread
              
How to protect song across thread?
 - Send only playback events to be renderer
 - Add mutexes
 - Copy song

Add CSS like instruments:
 - instrument-family: Electric Piano, Piano;
 - volume: 0.5;
 - sample: sample.wav;
 - detune: -3c;
 - what if we want to do the same for tracks?

Handle InputEvent correctly without offerInput

Make sure notes are in the correct octave (important)
 - "B3" and all of its possible variants (Bdouble flat, B♭, B, B♯, Bdouble sharp) would properly be designated as being in octave "3".
 - "C4" and all of its possible variants (Cdouble flat, C♭, C, C♯, Cdouble sharp) would properly be designated as being in octave "4".

- Breaking limitations
 - We are limited currently by the octave
   - Bohlen-Pierce scales
 - If we accept tritaves, can we split intervals in new ways
   - 2:1 = 3:2 * 4:3
   - 3:1 = 2:1 * 3:2
 - Or can we create intervals from octaves?
 - Fuzzy chords:
   - Do intervals have to be binary? Could we put in just a drop of minor third?
   - or have them pulse
   - or change the quality seamlessly   

Seamless chord changes
 - interpolate notes
 
Add aliases: Electric Piano == Electric Grand Piano
Add soundfont instruments by index to get the correct midi names

Add oversampling possibility
Add WaveShaper

Misc:
 - Copy drum patterns and everything from Microtonal christmas video

Add note mode
 - Glide to next node (Portamento?)
 - Release
 - Cut

Pattern specific keys do not really work
 - If we do a modulation, for example, from C -> D, the D should be the D from just tuning of C
 - We should have a command for retuning to a note
 - Unless there are child patterns.

Delay:
  - Lookup Delay parameters from some existing Delay effect
  - Refactor Delay to work like Chorus
  - Delay is sample rate dependant => convert delay to milliseconds

Chorus:
  - Use LFO

Pass Instrument as parameter to InstrumentVoice constructor, so that it can pick necessary fields
- OR add populate method

Add LFOState that keeps the samplerate etc.

MIDI:
 - Since GM2 has multiple percussion banks, we should be able to select a specific bank
 - are we sure, the percussion banks are always at the same presets?
   * really?
 - modify SoundFont brightness
 - Should we store midi control messages?
 
Consider adding laziness to mixing
 - If the channels have different positions, we could delay mixing
 - Of course, in a perfect world master channel location would translate it's children (+ rotate and scale)

LFO misses a step when period changes ?

Add pitch bend effect

Compression / Vocoder
 - How to make bass drum channel compress the lead?
 - Compress with bass drum and the lead as children (Pumping)
 - How to apply vocoder two two channels?
 - Pump pads on muted bass drum

SongState
 - add mixer settings
 - add export settings
 - add LFO states?

Add vocoder to vocode sample based on sequencer track
  
Other ratios in tonality cube:
 - 7:8:10 => 28:35:40
 - 6:7:8 => 21:24:28
 - 5:6:7 => 30:35:42
 
what is the difference between volume and gain? should we replace volume everywhere?

Tempo is fucked up => fix with metronome

Decrease CPU activity for inactive song or tracks:
 - If synth is not playing and no voices are active, stop playing at some point
 - If track output is empty, don't send it to mixer

- Rendering:
  * when rendering a song, select output type (stereo, binaural, 5.1)
  * find a container that could accept multiple sound format
  * possibly even render a video
  * add cover art
  * select bitrate
  * make render() methods const
  
- HRFT and mixing:
  - If accumulate distance is zero:
     - no HRFT is done, no panning is done
     - for HRFT mix the track to master
     - we would like to add stereo atmospheric effects directly, but what if such channel has distance?
        - do we mix the channels?
  - Perhaps we should keep track of channels and their positions
  - normal mixer could use distance as well as azimuth
  - if hrtf doesn't use distance, we could use it by ourselves
  
Recreate Täällä pohjantähden alla waltz

remove automatic scaling from volume meter
 - print maximum values to status

Can we split other than superparticular ratios? Like 5:3
 - 3:2 = 1+1/2 (G)
 - 1+2/3 = 5/3 (A)
 - 1+3/4 => 7/4 (A#4)
 - 1+4/5 => 9/5 (Bb4)
 - 1+5/6 => 11/6 (Ax4)
 - 1+7/8 => 15/8 (B-4)

 - 5/3 = 5:4 * 4:3
 - C F A
   D G B   ??
   E ? ?   ??
   F A D
   G B E   ??
   A D ?
   B ? ?   ??

Effects:
 - add delay modulator => delay is used to speed or slow input signal possibly with noise as the signal
 - lowpassed noise
   => modulate or just play
 - sidechain peak compression (Peak Controller from LMMS)
 - add tempo sync

Samples:
 - Record egg shell cracking
 - environment
   - raindrops on roof
   - waves crashing against shore
   - probably have multiple locations

Add table based editor for instruments and effects
 - tree based table
 - different hierarchies for different stuff:
   + channel hierarchy
   + channels and their filters

add putstrN method that limits printing and adds epplisis
  - needs better utf8 class

Try english mac keyboard to see what layout works

Instruments:
 - instrument system should have predefined slots show that if the track is set to electric piano, any electric piano is used unless user changes it
 - midi:electric_piano, midi:43

Use tab key to temporarily add note column for editing

- Find effects:
  - ZynPhaser
  - Calf Compressor
  - Panda
  - Calf Gate
  - ZamGEQ31

- Release:
  - refactor getSampleInterval() and fix tempo (rename it, samples per row)
  - Refactor Delay parameter names
  - Compressor
      
Add FM instruments:
// Harpsichord 7.8 3 5 24 0.01 0.8 0.0 0.1                                                                    
// Bell 3.5 7 9 0 0.01 0.2 0.3 1.5                                                                            
// Oboe 0.7 1 3 24 0.05 0.3 0.8 0.2

XML:
 - Add separate XML element for soundfont import
   - we can pick instruments
 - <instrumentSets>
     <instrumentSet src="file.sf2" default />
     <instrumentSet id="1" src="tabla.sf2"/>     
   </instrumentSets>

   <defaultInstrument id="1" class="midi:12" />
   <defaultInstrument id="2" class="midi:percussion:100" />   
   <fromSet set="1" class="midi:12" />
   
 - Import soundfont: <instruments><import src="file.sf2"/></instruments>
   or <instruments from="file.sf2"></instruments>
   
 - SoundFont instrument needs to be loaded without preloading the sf file or loading the sf for each instrument
 - standard instrument:
   <midiInstrument code="1" />
   <midiInstrument name="Muted Trumpet"/>
   <standardInstrument name="Muted Trumpet"/>

- SoundFont
  - Is there any data in channels or banks?
  - Lowpass filter is configured all over the place in SoundFont
    - Maybe add createLowpassFilter method
  - add exceptions
  - Add soundfont autodiscovery
    + check where Ubuntu stores soundfonts
    + /usr/share/soundfonts/, /usr/share/sounds/sf2/ (and /usr/local/)
  - Add separate Envelope that holds parameters in cents
  - Generate hold and decay parameters for SoundFont enevelope states in SoundFont
  - Since render interval is pattern dependant, soundfont effect rate can change
    - what?
  - SoundFont crashes if file is not found
  - move pitchRatio from SoundFontVoice data to InstrumentVoice
    - also pitch calculation
  - envelope setup uses apparent key
  - banks? there are multiple drum sets in banks and we might want to switch them
  - move all filters to common
  - make sure voice doesn't jump from attack to release
  - handle input messages
    => volume control
    => pitch?
    => remove playNote and stopNote and just have everything handle TrackEvents
  - consider doing lowpass at the end of soundfont rendering
    - gain might be problem since its currently applied before lowpass
    - since lowpass is updated all the time that might be a problem

- add weird filters
  - filter note data
    - arpeggiator?
  - render to buffer so that song can mangled

- make filters and instruments arrays so that they can process multiple tracks at the same time and use SIMD

- nonlinear songs
  - song as a graph?

- we want smooth distortion:
  - interesting distortion was achieved by adding input to uncleared data
  - clear samples from signal
    - use periodicity
  - try applying lowpass after distortion

- Study effects / instruments in other software:
  - Audacity
  - LMMS has multiple interesting instruments such as vibrating string
    
- Renoise
  -  Add groove
    - add delay to every nth line
    - part of time signature?
  - Add Track headroom in desibels (default -6db)
  - add shortcut for add note column
  - add track collapse
  - add track colors
  - ESC => edit mode
  - End note with capslock
  - If recordings can be copied from track to track, shouldn't notes also have to be able to be copied while retaining the instrument
    - If recordings were to be copied to different track, we could create a new subtrack for the recording
  - Backspace: Delete all notes and Effect Commands at the current line in the track and scroll everything beneath the current line up.
  - Insert: Insert clean row into track and push all notes and effects in current track down.
  - Left Ctrl & 1-0: Set the Edit Step number.
  - select by holding left shift
  - Home/End: Go to start/end line of pattern.
  - add ticks per line (default 12 - delay has 256)
  - consider using tab:
    - tab => next note column
    - shift tab => prev note column
  - keybindings:
      play from playhead position: shift space bar
      octave down / up: command [ / command ]
      collapse / expand track: shift command k
      begin selection: commmand b
      end selection: command e
      Transpose One Note Up: fn option F2
      Transpose One Note Down: fn option F1
      Transpose One Octave Up: fn option F12
      Transpose One Octave Down: fn option F11

- update plane dimensions on resize
  - every plane should handle the resize input

- add scratching mode:
  - should be possible to stratch any channel, but how? scratching institutes a new time
  - subsongs
    
- add recording
  - remove parts of recording using backspace
  - also copy parts of recording to different tracks
  - recordings must therefore be global to the song
  - Add playing of the recorded sample
  - Notes should have the sample position as value
  - recording should automatically add new notes
  - Should the sample still be an instrument?
  - Samples should also have multiple columns
  - Effect column should be available for samples
  - Single track can have multiple sample tracks but only single instrument, so instrument cannot be used for sample selection
    * but should a track have instrument per column? That way we could have all percussion in the same track
  
- Latency:
  - track latency: int snd_pcm_avail_delay(snd_pcm_t * pcm, snd_pcm_sframes_t * availp, snd_pcm_sframes_t * delayp)
  - https://www.alsa-project.org/main/index.php/Test_latency.c

- keep track of current song and refresh InstrumentList and ScoreDisplay if it changes
- implement chart natively
- add table that layouts planes automatically and draws the borders
- starting a reader, loses plane ownership
- ask for rerender
- move TerminalUI population to base class
- add generic parameters and window for editing
- add EditablePlane and use it for column headings => they can be clicked and edited

- remove sample rate constants (44100, 22050)
- song.gettime() doesn't account for tempo changes
  - should integrate the time in moveForward and moveForwardSample

- add note slide

chords:
  - minor 7th: root, minor third, perfect fifth, and minor seventh

Harmonic seventh chord: P1 M3 P5 7:4

Major pentatonic scale: P1 M2 M3 P5 M6 P8
Minor pentatonic scale: P1 m3 P4 P5 m7 P8 (for m3: 32:27, for m7: 16:9)
Blues scale:		P1 m3 P4 ♭5 P5 m7 P8

						3-limit		5-limit		7-limit
C	P1	I	Unison			1:1		1:1		1:1
D♭♭	d2	♭♭II	Diminished second			128:125		36:35
C♯		♯I	Chromatic semitone			25:24		21:20
D♭	m2	♭II	Minor second				16:15, 27:25	15:14
D	M2	II	Major second		9:8		10:9
E♭♭	d3	♭♭III	Diminished third			144:125		8:7
D♯	A2	♯II	Augmented second			75:64, 125:108	7:6
E♭	m3	♭III	Minor third		32:27		6:5
E	M3	III	Major third				5:4
F♭		♭IV	Diminished fourth			32:25		9:7
E♯		♯III	Augmented third				125:96		21:16
F	P4	IV	Perfect fourth		4:3		27:20
F♯		♯IV	Augmented fourth			25:18, 45:32	7:5
G♭		♭V	Diminished fifth			36:25, 64:45	10:7
G	P5	V	Perfect fifth		3:2		40:27
A♭♭		♭♭VI	Diminished sixth			192:125		32:21
G♯		♯V	Augmented fifth				25:16		14:9
A♭	m6	♭VI	Minor sixth				8:5
A	M6	VI	Major sixth				5:3
B♭♭		♭♭VII	Diminished seventh			128:75, 216:125	12:7
A♯		♯VI	Augmented sixth				125:72		7:4
B♭	m7	♭VII	Minor seventh		16:9		9:5
B	M7	VII	Major seventh				15:8 (50:27)	28:15
Cy		♭VIII	Diminished octave			48:25		40:21
B♯		♯VII	Augmented seventh			125:64		35:18
C	P8		Octave			2:1		2:1		2:1
	m9		Minor ninth				32:15 ?
D'	M9		Major ninth		9:4
	m10
D'♯ / E'♭							75:32
E'	M10		Major tenth				5:2
F'	P11
G'	P12					3:1
	m13
	M13
	m14
	P15

Other:

7/6  septimal minor third
9/7  septimal major third
5/7   septimal minor ninth, BP ninth
16/7   septimal major ninth
17/14  supraminor third


---

Chords

I	C	P1 M3 P5
II	Dm	M2 P4 M6
III	Em	M3 
IV	F	P4 M6 P8
V	G
VI	Am	M6 P8 M10
VIIo	Bdim

------------------

Constructions:

2-limit biad => 1:1 2:1 => C C'

3-limit minor triad => 3:2 2:1 1:1 => G C' C
3-limit major triad => 4:3 2:1 1:1 => F C' C

5-limit major triad => 5:4 3:2 2:1 1:1 => E G C' C
5-limit minor triad => 6:5 3:2 2:1 1:1 => Eb G C' C

7-limit major triad => 7:6 4:3 2:1 1:1 => D# F C' C
7-limit minor triad => 8:7 4:3 2:1 1:1 => E♭♭ F C' C

11-limit major tetrad => 11:10 6:5 3:2 2:1 1:1 => ? Eb G C' C
11-limit minor tetrad => 12:11 6:5 3:2 2:1 1:1 => ? Eb G C' C

17-limit => 17:16 9:8
17-limit => 18:17 9:8

7-limit subminor triad ratio:  42:48:56

--

7/8
12/7

--

3-limit chords:

I	C C' G'
i	C G G'
II	G G' D''
ii	G D' D''
III	D' D'' 27:8

--

